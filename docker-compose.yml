services:
  app:
    build: .
    volumes:
      - ./data/logs:/app/logs
    ports:
      - "8080:8080"

  db:
    image: postgres:18.4-bookworm
    ports:
      - "5432:5432"
    volumes:
      - ./data/postgres:/var/lib/postgresql